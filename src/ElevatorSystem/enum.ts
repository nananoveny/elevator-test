export enum DOOR_STATUS {
  OPEN,
  CLOSE,
}

export enum ELEVATOR_STATUS {
  MOVING,
  FULL,
  WAIT,
}

export enum DIRECTION {
  UP,
  DOWN,
  IDLE,
}

export enum REQUEST_TYPE {
  INSIDE,
  OUTSIDE,
  DUAL,
}
